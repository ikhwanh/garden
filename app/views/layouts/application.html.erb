<!DOCTYPE html>
<html class="h-full bg-gray-50">
  <head>
    <title><%= content_for(:title) || "Garden" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Garden">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="h-full flex flex-col">
    <nav class="shrink-0 bg-white border-b border-gray-200">
      <div class="px-4 sm:px-6 h-12 flex items-center justify-between">
        <%= link_to "Garden", root_path, class: "text-base font-bold text-gray-900 tracking-tight" %>

        <div class="flex items-center gap-4">
          <% if user_signed_in? %>
            <span class="text-xs text-gray-400 hidden sm:inline"><%= current_user.email %></span>
            <%= link_to "Settings", edit_user_registration_path,
                  class: "text-sm font-medium text-gray-600 hover:text-gray-900" %>
            <%= button_to "Sign out", destroy_user_session_path, method: :delete,
                  class: "text-sm font-medium text-gray-600 hover:text-gray-900 bg-transparent border-0 cursor-pointer px-0" %>
          <% else %>
            <%= link_to "Sign in", new_user_session_path,
                  class: "text-sm font-medium text-gray-700 hover:text-gray-900" %>
            <%= link_to "Sign up", new_user_registration_path,
                  class: "inline-flex items-center px-3 py-1.5 bg-gray-900 text-white text-sm font-medium rounded-md hover:bg-gray-700 transition-colors" %>
          <% end %>
        </div>
      </div>
    </nav>

    <% if user_signed_in? %>
      <div class="flex flex-1 min-h-0">
        <aside class="w-44 shrink-0 bg-white border-r border-gray-200 overflow-y-auto">
          <nav class="p-2 space-y-0.5">
            <% dashboard_active = request.path.start_with?("/dashboard") %>
            <%= link_to dashboard_monitoring_path,
                class: "flex items-center gap-2 px-3 py-2 text-sm font-medium rounded-md #{dashboard_active ? 'text-gray-900' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'}" do %>
              <span>&#127968;</span>Dashboard
            <% end %>
            <% if dashboard_active %>
              <% [
                   [dashboard_monitoring_path, "Monitoring"],
                   [dashboard_finance_path,    "Finance"],
                   [dashboard_tools_path,      "Tools"]
                 ].each do |path, label| %>
                <%= link_to label, path,
                    class: "flex items-center pl-9 pr-3 py-1.5 text-xs font-medium rounded-md #{request.path == path ? 'bg-gray-100 text-gray-900' : 'text-gray-500 hover:bg-gray-50 hover:text-gray-700'}" %>
              <% end %>
            <% end %>

            <% nurseries_active = request.path.start_with?("/nurseries") %>
            <%= link_to nurseries_path,
                class: "flex items-center gap-2 px-3 py-2 text-sm font-medium rounded-md #{nurseries_active ? 'bg-gray-100 text-gray-900' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'}" do %>
              <span>&#127793;</span>Nurseries
            <% end %>

            <% crops_active = request.path.start_with?("/crops") %>
            <%= link_to crops_path,
                class: "flex items-center gap-2 px-3 py-2 text-sm font-medium rounded-md #{crops_active ? 'bg-gray-100 text-gray-900' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'}" do %>
              <span>&#127807;</span>Crops
            <% end %>

            <% presets_active = request.path.start_with?("/presets") %>
            <%= link_to presets_path,
                class: "flex items-center gap-2 px-3 py-2 text-sm font-medium rounded-md #{presets_active ? 'bg-gray-100 text-gray-900' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'}" do %>
              <span>&#9881;&#65039;</span>Presets
            <% end %>

            <% cashflow_active = request.path.start_with?("/cashflow") %>
            <%= link_to cashflow_path,
                class: "flex items-center gap-2 px-3 py-2 text-sm font-medium rounded-md #{cashflow_active ? 'bg-gray-100 text-gray-900' : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'}" do %>
              <span>&#128184;</span>Cashflow
            <% end %>
          </nav>
        </aside>

        <main class="flex-1 min-w-0 overflow-hidden flex flex-col">
          <% if notice.present? %>
            <div class="px-4 pt-3" data-turbo-temporary>
              <div class="flex items-center gap-3 px-4 py-2.5 bg-green-50 border border-green-200 text-green-800 text-sm rounded-lg">
                <svg class="w-4 h-4 shrink-0" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd"/></svg>
                <%= notice %>
              </div>
            </div>
          <% end %>

          <% if alert.present? %>
            <div class="px-4 pt-3" data-turbo-temporary>
              <div class="flex items-center gap-3 px-4 py-2.5 bg-red-50 border border-red-200 text-red-800 text-sm rounded-lg">
                <svg class="w-4 h-4 shrink-0" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.28 7.22a.75.75 0 00-1.06 1.06L8.94 10l-1.72 1.72a.75.75 0 101.06 1.06L10 11.06l1.72 1.72a.75.75 0 101.06-1.06L11.06 10l1.72-1.72a.75.75 0 00-1.06-1.06L10 8.94 8.28 7.22z" clip-rule="evenodd"/></svg>
                <%= alert %>
              </div>
            </div>
          <% end %>

          <div class="flex-1 min-h-0 overflow-hidden">
            <%= yield %>
          </div>
        </main>
      </div>
    <% else %>
      <div class="flex-1 overflow-y-auto">
        <% if notice.present? %>
          <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 mt-4" data-turbo-temporary>
            <div class="flex items-center gap-3 px-4 py-3 bg-green-50 border border-green-200 text-green-800 text-sm rounded-lg">
              <svg class="w-4 h-4 shrink-0" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 001.137-.089l4-5.5z" clip-rule="evenodd"/></svg>
              <%= notice %>
            </div>
          </div>
        <% end %>

        <% if alert.present? %>
          <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 mt-4" data-turbo-temporary>
            <div class="flex items-center gap-3 px-4 py-3 bg-red-50 border border-red-200 text-red-800 text-sm rounded-lg">
              <svg class="w-4 h-4 shrink-0" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.28 7.22a.75.75 0 00-1.06 1.06L8.94 10l-1.72 1.72a.75.75 0 101.06 1.06L10 11.06l1.72 1.72a.75.75 0 101.06-1.06L11.06 10l1.72-1.72a.75.75 0 00-1.06-1.06L10 8.94 8.28 7.22z" clip-rule="evenodd"/></svg>
              <%= alert %>
            </div>
          </div>
        <% end %>

        <%= yield %>
      </div>
    <% end %>
  </body>
</html>
